# Legacy MySQL SQL converter fixtures

These files are retained only as source material for `scripts/generate_pg_init.py`.
The deployable Jimmer demo does **not** execute these MySQL scripts directly.
Use `sql/postgresql/00-create-database.sql` and
`sql/postgresql/ruoyi-warm-flow-jimmer-postgres.sql` for PostgreSQL deployment.
